Hier finden sie den Code der hinter der exe steckt. 
IDs.csv und key.key wird benötigt um auf die datenbank zuzugreifen. Diese werden allerdings nicht bei der exe benötigt,
da diese durch die secure.exe erstellt wird.
Die crypt.py Datei verschlüsselt die Benutzerzugangsdaten zur SQL-Datenbank.